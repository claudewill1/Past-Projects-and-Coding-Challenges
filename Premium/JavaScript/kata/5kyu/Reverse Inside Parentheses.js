const reverseInParens = text=>{
    let i = 0, backArr = [], recursiveArr = [], a = [],
    numOfBrack = -1, alteredString = text, chars = '()';

    for(let char in chars){
        alteredString = alteredString.replace(char,'');
    }
}